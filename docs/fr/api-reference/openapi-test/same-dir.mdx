---
title: 'Référence dans le même répertoire'
description: 'Référence la spec colocalisée dans le même répertoire que ce MDX'
openapi: 'local-spec.json GET /items'
---

Référence `local-spec.json` qui se trouve dans le même répertoire que cette page. Chemin depuis la racine du projet : `docs/api-reference/openapi-test/local-spec.json`.
